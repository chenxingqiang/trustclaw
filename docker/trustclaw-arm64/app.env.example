# TrustClaw Docker 运行配置（复制为 app.env 后修改）
# 参考：选手电脑 ARM64 Docker 应用部署操作说明
# API Key 放 app.env.dev（见 app.env.dev.example），compose 会自动叠加加载。

# 宿主机映射端口（Control UI / Gateway API：http://<host>:8080/）
APP_PORT=8080

# PTDS Runtime Console（Docker 宿主机端口，避开本地 dev :5174）
# http://127.0.0.1:15174/trustclaw/
TRUSTCLAW_UI_PORT=15174

# Gateway 容器内端口（保持 19001，与 TrustClaw 默认一致）
OPENCLAW_GATEWAY_PORT=19001

# Control UI / API 访问令牌（请改成强随机值）
OPENCLAW_GATEWAY_TOKEN=change-me-trustclaw-demo

# 主聊天模型（Sonnet）
OPENCLAW_PRIMARY_MODEL=anthropic/claude-sonnet-4-6

# 默认领域 Agent Pack
TRUSTCLAW_DEFAULT_AGENT_PACK=glp1-eligibility

# Control UI 浏览器来源（逗号分隔，可选；默认按 APP_PORT / TRUSTCLAW_UI_PORT 生成）
# TRUSTCLAW_CONTROL_UI_ORIGINS=http://127.0.0.1:8080,http://localhost:8080

TZ=Asia/Shanghai
